<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose drives all colours at runtime; this base only needs to be a
         Material 3 / edge-to-edge friendly theme with no action bar. -->
    <style name="Theme.NetSpeedIndicator" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
